<% include ../partials/header %>

    <div class="row">
        <h1 style="text-align: center">Редактирование комментария</h1>
        <div style="width: 30%; margin: 25px auto;">
            <form action="/cafes/<%= cafe_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <textarea class="form-control" rows="5" name="comment[text]" ><%= comment.text %></textarea>
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Отправить!</button>
                </div>
            </form>
            <a href="/cafes">Назад</a>
        </div>
    </div>

<% include ../partials/footer %>